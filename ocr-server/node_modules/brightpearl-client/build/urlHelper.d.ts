declare class UrlHelper {
    static getUrl(url: string, elements: ElementsToReplace[]): string;
}
interface ElementsToReplace {
    key: string;
    value: string;
}
export { UrlHelper, ElementsToReplace };
